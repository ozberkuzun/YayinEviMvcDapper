@model IEnumerable<YayınEviMvcDapper.Models.YayinEviModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Yeni Ekle", "EY")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.YayinEviAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.YayinEviAdresi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.YayinEviAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.YayinEviAdresi)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "EY", new { id = item.YayinEviNo }) |

            @Html.ActionLink("Sil", "Delete", new { id = item.YayinEviNo })
        </td>
    </tr>
}

</table>
